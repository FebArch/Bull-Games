<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<style>

    body{
      background-color: #15161a;
      color: #ffc107;
    }
    .nav-link, .navbar-brand{
        display: inline-block; /* Ensures the anchor behaves like a block element */
        transition: transform 0.3s ease;
    }

    .nav-link:hover, .navbar-brand:hover{
        color: #ffc107 !important;
        transform: scale(1.2);
        font-weight: 500;
    }

    .custom-select {
      max-height: 200px !important;
      overflow-y: auto;
    }

    .image-container {
  position: relative;
  display: inline-block;
}

.image-container img {
  width: 100%; /* Ensure the image scales properly */
  height: auto; /* Maintain aspect ratio */
}

.overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(25deg, rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0)); /* Black to transparent gradient */
  pointer-events: none; /* Ensures the overlay does not interfere with interactions on the image */
}


  .li-no-border{
    border: none;
  }
    /* Style to change checkbox color to yellow when checked */
    .form-check-input:checked {
      background-color: #ffc107;
      border-color: #ffc107;
    }

    .form-check-input:checked:focus {
      box-shadow: 0 0 0 0.25rem rgba(255, 255, 0, 0.5); /* Optional, for a glow effect */
    }


footer h5{
  color: #ffc107;

}

</style>